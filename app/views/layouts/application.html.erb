<!DOCTYPE html>
<html>
	<head>
	 <title><%= full_title(yield(:title)) %></title> <!--yieldメソッドでprovideの値を受け取れる。-->
		<%= csrf_meta_tags %>

		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
	
		<%= render 'layouts/shim' %> <!--renderメソッドは呼び出すテンプレート（ビュー）ファイルを指定できる-->
	  <!--この場合layoutsフォルダーの_shim.html.erbを指定して呼び出す。-->
	</head>
	
	<body>
	  <%= render 'layouts/header' %>
		  <div class="container">
		      <% flash.each do |message_type, msg| %>
		      <!--message_type, msgのmessage_typeにはハッシュキー、msgにはバリューが入る。-->
            <div class="alert alert-<%= message_type %>"><%= msg %></div>
            
          <% end %>
			  <%= yield %>
			  <%= debug(params) if Rails.env.development? %>
			  <!--Rails.envはdevelopment(開発環境)とproduction(本番環境)、どちらなのかを
			  確認する。この場合はもし開発環境ならdebugにparams引数を渡して表示させる-->
			</div>
	</body>
</html>
